name = "weighted-erdos-szekeres"
defaultTargets = ["WeightedErdosSzekeres"]

[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.29.0"

[[lean_lib]]
name = "WeightedErdosSzekeres"

# Standalone eval targets extracted from FAILURE_ATLAS.md. They contain
# intentional `sorry`s, so they are *not* in `defaultTargets`; build them
# explicitly with `lake build Benchmarks`.
[[lean_lib]]
name = "Benchmarks"
